# Builder Current i() Alias A/B — Post-176

- Adds exactly one recovery compile-ref typed alias at p$a.
- Run229 removed all 44 conflicting concrete builder i() source bridges; residual=0.
- Donor provider-map proves p$a synthetic i()->x$a delegates to typed ai()->p$a with TBuilderType Signature.
- a$a i() path is intentionally not exposed because its target d() is abstract.
- Existing donor methods/source/gameplay unchanged.
