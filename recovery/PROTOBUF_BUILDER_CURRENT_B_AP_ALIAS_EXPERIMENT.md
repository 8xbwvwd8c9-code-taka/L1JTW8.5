# Builder Current b(ap) Alias A/B

- Adds exactly one recovery compile-ref typed alias at p$a.
- Source collision scan: zero exact b(ap) declarations.
- Uses nearest p$a donor bridge -> typed d(ap)->p$a / TBuilderType.
- Does not expose duplicate a$a b(ap) alias.
- Existing donor methods/source/gameplay unchanged.
