# BUG-850-289 Validation — 2026-09-24

## Result

- `STATUS=PASS`
- `SEVERITY=L2`
- `PROMOTION=COMPLETE`
- `COMPLETED_BRANCH=completed/l1jtw85-core-fixes`
- `PROMOTION_COMMIT=712eb64cc743e2f5a0631ab9211ccbe11e1e30e7`

## Defect

Marriage/divorce `characters.PartnerID` updates were bilateral state changes but could be persisted as independent operations. A partial failure could leave the two character rows, RAM state, or online/offline spouse handling inconsistent.

## Repair authority

The complete BUG-850-289 contract is the union of two validated work-stage repairs:

1. `097679d518fb5306401140de3c2bf111c57a915d` — atomically persist marriage PartnerID pair.
2. `ff0417e4a16149376462092083a630f225b343dc` — extend the same contract to atomic divorce / offline spouse handling.

The final contract uses one DB transaction, ordered row locking with `FOR UPDATE`, expected-value guarded row updates, rollback on any failure, and publishes RAM state only after commit.

## Completed promotion scope

Exactly four runtime source paths were promoted:

- `recovered-src-obf/aj/f.java`
- `recovered-src-obf/ao/o.java`
- `recovery/normalized-src-vf/l1r/aj/C_Attr.java`
- `recovery/normalized-src-vf/l1r/ao/CharacterTable.java`

No temporary workflow file was promoted.

## Validation evidence

Temporary integration run `35968194100` (`TMP Complete BUG-850-289 Promotion`) completed successfully before promotion.

Successful gates:

- validated divorce closure applied cleanly
- authoritative BUG-850-289 source contract PASS
- Java 8 normalized no-new-regression PASS
- authoritative runtime model PASS
- validated complete patch commit PASS

The clean completed commit was rebuilt from the exact four validated source blobs and compared against its completed parent; the diff contains only those four files.

## Contract closure

- Marriage: both PartnerID rows persist atomically.
- Divorce: both PartnerID rows clear atomically.
- Online/offline spouse paths share the same durable transaction boundary.
- Non-reciprocal/conflicting relations fail closed.
- DB failure rolls back the pair.
- RAM PartnerID state is changed only after durable commit.

`BUG-850-289=PASS`
