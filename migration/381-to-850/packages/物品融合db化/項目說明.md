# 物品融合db化 — 物品融合db化

## 企劃來源
- 天堂企劃.txt 第4條：`L2,w_物品融合db化,N/A`
- 備注：適合暗黑系統 先暫存

## 決策
**HOLD** — 功能尚未在850核心中實作，暫存評估。

## 381 資料庫狀態
| 欄位 | 值 |
|------|----|
| 381 SQL 檔案 | `w_物品融合db化_202609221205.sql` |
| 檔案大小 | **0 bytes**（無資料，BLOCKED） |
| 結構來源 | `atu381_0906.sql`（備用） |
| 資料狀態 | NO_ACTIVE_DATA |

### 381 Schema（從 atu381_0906.sql 取得）
```sql
CREATE TABLE `w_物品融合db化` (
  `item_id`             int(11)              NOT NULL,
  `name`                varchar(45)          NOT NULL,
  `checkClass`          int(11)              NOT NULL DEFAULT '0',
  `checkLevel`          int(11)              NOT NULL DEFAULT '0',
  `rnd`                 int(11)              NOT NULL DEFAULT '100',
  `checkItem`           int(11)              NOT NULL,
  `hpConsume`           int(10) unsigned     NOT NULL DEFAULT '0',
  `mpConsume`           int(10) unsigned     NOT NULL DEFAULT '0',
  `material`            int(11)              NOT NULL,
  `material_count`      int(11)              NOT NULL,
  `material_2`          int(11)              NOT NULL,
  `material_2_count`    int(11)              NOT NULL,
  `material_3`          int(11)              NOT NULL,
  `material_3_count`    int(11)              NOT NULL,
  `material_4`          int(11)              NOT NULL,
  `material_4_count`    int(11)              NOT NULL,
  `material_5`          int(11)              NOT NULL,
  `material_5_count`    int(11)              NOT NULL,
  `new_item`            int(11)              NOT NULL,
  `new_item_counts`     int(11)              NOT NULL,
  `new_Enchantlvl_SW`   int(11)              NOT NULL,
  `new_item_Enchantlvl` int(11)              NOT NULL DEFAULT '0',
  `removeItem`          int(11)              NOT NULL DEFAULT '1',
  `message`             varchar(1000)        DEFAULT NULL,
  `item_Html`           int(11)              NOT NULL,
  PRIMARY KEY (`item_id`) USING BTREE
) ENGINE=MyISAM DEFAULT CHARSET=utf8;
```

## 850 等效狀態
- 850 `db/8.5.sql` 中**無此資料表**
- 缺口狀態：**明確缺口**，需新增資料表

## 功能說明
物品融合系統：玩家使用指定材料道具，透過 NPC 或 HTML 介面，合成產出新道具。支援最多 5 種材料、成功率(rnd)、職業限制、等級限制、HP/MP 消耗。

## 阻斷因素
- BLOCKED：381 SQL 為 0 bytes，無法直接取得實際配置資料
- 備用：atu381_0906.sql 提供 CREATE TABLE 結構，但無 INSERT 資料
- 暗黑系統整合尚未規劃，需先確定850暗黑系統方向

