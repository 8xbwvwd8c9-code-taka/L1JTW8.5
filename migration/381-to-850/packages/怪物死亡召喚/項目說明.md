# 項目說明 — 怪物死亡召喚（Monster Death Spawn）

## 1. 基本資訊

| 欄位 | 值 |
|------|-----|
| **項目名稱** | 怪物死亡召喚 |
| **別名** | w_怪物死亡召喚 / Monster Death Spawn / 怪物死亡復活強化 |
| **企劃來源** | `I:\天堂企劃.txt` 第 31~37 行：`L2,w_怪物死亡召喚,N/A` |
| **清冊 ID** | `w_monster_death_spawn`、`_config` |
| **module_id** | `怪物死亡召喚` |
| **企劃規格** | 低機率小怪 1% / BOSS 0.5%；死亡後在角色周邊復活並加強 1.5% 能力；死亡後有再次觸發機率能力再度強化直到 100% 強化度；每次復活掉落率增加 1% |

---

## 2. 功能機制與規則

1. **觸發機率**：
   - 一般小怪：**1.0%**（`trigger_chance_x10 = 10`）
   - BOSS 級怪物：**0.5%**（`trigger_chance_x10 = 5`）
2. **能力強化累積**：
   - 每次復活強化 **+1.5%**（`strength_pct_per_spawn = 150`）
   - 包含 HP 上限擴增、物理防禦/傷害減免提升
   - 強化上限：**+100.0%**（即最大強化度封頂，達到 100% 後不再觸發復活）
3. **掉落率加成累積**：
   - 每次復活額外 **+1%** 掉落率加成（`drop_bonus_pct_per_spawn = 1`）
   - 在掉落結算（`DropTable`）時，掉落率乘數乘以 `(1.0 + deathSpawnDropBonusPct / 100.0)`
4. **召喚位置與特效**：
   - 預設在擊殺者（玩家/寵物主人）周邊半徑 2 格隨機尋找可站立座標召喚
   - 播放復活召喚光效（`S_SkillSound` 7771）與喊話（`death_talk`）
5. **DB 開關與控制**：
   - `_config` 表中加入：
     - `MonsterDeathSpawnSwitch`：總開關（0=關閉, 1=開啟，預設 1）
     - `MonsterDeathSpawnGlobalMobChance`：全域小怪機率（預設 10 = 1.0%）
     - `MonsterDeathSpawnGlobalBossChance`：全域 BOSS 機率（預設 5 = 0.5%）

---

## 3. 資料庫 Schema 設計

### 3.1 規則表：`w_monster_death_spawn` (InnoDB, utf8)
```sql
CREATE TABLE IF NOT EXISTS `w_monster_death_spawn` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `dead_npc_id` int(11) NOT NULL COMMENT '死亡NPC編號 (-1=全域小怪預設, -2=全域BOSS預設, >0為指定NPC)',
  `note` varchar(100) CHARACTER SET utf8 DEFAULT NULL COMMENT '備註說明',
  `spawn_npc_id` int(11) NOT NULL DEFAULT 0 COMMENT '召喚NPC編號 (0=原地召喚同一種怪物)',
  `spawn_duration_min` int(11) NOT NULL DEFAULT 0 COMMENT '召喚怪物存活時間(分鐘, 0=自然存活)',
  `death_talk` varchar(255) CHARACTER SET utf8 NOT NULL DEFAULT '' COMMENT '怪物死亡時的喊話或廣播',
  `tele_x` int(11) DEFAULT 0 COMMENT '指定傳送X (-1=不傳送, 0=在擊殺者身邊隨機召喚)',
  `tele_y` int(11) DEFAULT 0 COMMENT '指定傳送Y (-1=不傳送, 0=在擊殺者身邊隨機召喚)',
  `tele_mapid` int(11) DEFAULT -1 COMMENT '指定傳送地圖ID (-1=擊殺者當前地圖)',
  `is_boss` tinyint(1) NOT NULL DEFAULT 0 COMMENT '0=一般小怪, 1=BOSS級怪物',
  `trigger_chance_x10` int(5) NOT NULL DEFAULT 10 COMMENT '觸發機率x10 (10=1.0%, 5=0.5%)',
  `strength_pct_per_spawn` int(5) NOT NULL DEFAULT 150 COMMENT '每次復活能力強化值%x100 (150 = +1.5%)',
  `max_strength_pct` int(5) NOT NULL DEFAULT 10000 COMMENT '最大累積強化度上限%x100 (10000 = +100%)',
  `drop_bonus_pct_per_spawn` int(3) NOT NULL DEFAULT 1 COMMENT '每次復活掉落率加成% (1 = +1%)',
  PRIMARY KEY (`id`),
  KEY `idx_dead_npc` (`dead_npc_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

---

## 4. 核心類別與雙重交付清單 (Double Delivery)

1. **核心類別修改與新增**：
   - `l1r.ao.MonsterDeathSpawnTable`：新建規則表快取管理者，載入 `w_monster_death_spawn`
   - `l1r.l1j.server.Config`：載入 `MonsterDeathSpawnSwitch` 與全域機率
   - `l1r.ap.L1MonsterInstance`：新增強化與掉落累積欄位、死亡 hook (`handleMonsterDeathSpawn`)
   - `l1r.ao.DropTable`：結算時乘上死亡召喚掉落加成
2. **交付位置**：
   - **支線放一顆**：提交於 `work/feature-d-monster-elite` 分支中。
   - **文件夾放一顆**：
     - 源檔：`migration/381-to-850/packages/怪物死亡召喚/850匯入/server/java/`
     - 二進制：`migration/381-to-850/packages/怪物死亡召喚/850匯入/server/class/`

---

## 5. 狀態與決策

```text
DECISION=HOLD
SERVER_LEVEL=L3 (怪物死亡召喚 + 1.5%能力強化累積至100%上限 + 每次+1%掉落加成 + 玩家周邊動態生成)
CLIENT_GATE=NOT_APPLICABLE (純伺服端生成與標準特效)
CONFIG_ENTRIES=_config (MonsterDeathSpawnSwitch, MonsterDeathSpawnGlobalMobChance, MonsterDeathSpawnGlobalBossChance)
DOUBLE_DELIVERY=CONFIRMED (branch + package server/java + server/class)
LAST_UPDATED=2026-09-25
```
