name: L1JTW8.5 L3 Completed Validation

on:
  push:
    branches:
      - completed/l1jtw85-core-fixes

permissions:
  contents: write

jobs:
  l3-completed-contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Merge protected L3 clan deletion transaction
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path

          def replace_section(path, start_marker, end_marker, new_section):
              p = Path(path)
              text = p.read_text(encoding="utf-8")
              if text.count(start_marker) != 1 or text.count(end_marker) != 1:
                  raise SystemExit(f"PATCH_GATE_FAIL {path}: section markers not unique")
              start = text.index(start_marker)
              end = text.index(end_marker, start)
              if end <= start:
                  raise SystemExit(f"PATCH_GATE_FAIL {path}: bad section order")
              text = text[:start] + new_section + text[end:]
              p.write_text(text, encoding="utf-8")

          replace_section(
              "recovery/normalized-src-vf/l1r/ao/ClanTable.java",
              "   public void b(String var1) {",
              "\n\n   public L1Clan a(int var1)",
              '''   public void b(String var1) {
      L1Clan var2 = this.c(var1);
      if (var2 == null) {
         return;
      }

      Connection var3 = null;
      try {
         var3 = DatabaseFactory.a().b();
         var3.setAutoCommit(false);
         try (PreparedStatement var4 = var3.prepareStatement("DELETE FROM clan_warehouse_history WHERE clan_id=?")) {
            var4.setInt(1, var2.e());
            var4.executeUpdate();
         }
         try (PreparedStatement var5 = var3.prepareStatement("DELETE FROM clan_data WHERE clan_name=?")) {
            var5.setString(1, var1);
            var5.executeUpdate();
         }
         var3.commit();
      } catch (SQLException var9) {
         if (var3 != null) {
            try {
               var3.rollback();
            } catch (SQLException ignored) {
            }
         }
         a.log(Level.SEVERE, var9.getLocalizedMessage(), var9);
         return;
      } finally {
         if (var3 != null) {
            try {
               var3.setAutoCommit(true);
            } catch (SQLException ignored) {
            }
         }
         SQLUtil.a(var3);
      }

      var2.c().g();
      var2.c().b();
      this.b.remove(var2.e());
   }'''
          )

          replace_section(
              "recovered-src-obf/ao/q.java",
              "    public void b(String clan_name) {",
              "\n\n    public i a(int clan_id)",
              '''    public void b(String clan_name) {
        i clan = this.c(clan_name);
        if (clan == null) {
            return;
        }
        Connection con = null;
        try {
            con = l1j.server.b.a().b();
            con.setAutoCommit(false);
            try (PreparedStatement pstm = con.prepareStatement("DELETE FROM clan_warehouse_history WHERE clan_id=?")) {
                pstm.setInt(1, clan.e());
                pstm.executeUpdate();
            }
            try (PreparedStatement pstm = con.prepareStatement("DELETE FROM clan_data WHERE clan_name=?")) {
                pstm.setString(1, clan_name);
                pstm.executeUpdate();
            }
            con.commit();
        }
        catch (SQLException e2) {
            if (con != null) {
                try {
                    con.rollback();
                }
                catch (SQLException ignored) {
                }
            }
            a.log(Level.SEVERE, e2.getLocalizedMessage(), e2);
            return;
        }
        finally {
            if (con != null) {
                try {
                    con.setAutoCommit(true);
                }
                catch (SQLException ignored) {
                }
            }
            j.a(con);
        }
        clan.c().g();
        clan.c().b();
        this.b.remove(clan.e());
    }'''
          )
          PY
          git diff --check
          if ! git diff --quiet -- recovery/normalized-src-vf/l1r/ao/ClanTable.java recovered-src-obf/ao/q.java; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add recovery/normalized-src-vf/l1r/ao/ClanTable.java recovered-src-obf/ao/q.java
            git commit -m "fix(l3): merge clan deletion transaction with completed hardening"
            git push origin HEAD:completed/l1jtw85-core-fixes
          fi
      - name: Run Batch1-7 source contracts
        shell: bash
        run: |
          set -euo pipefail
          for N in 1 2 3 4 5 6 7; do
            echo "=== BATCH$N ==="
            python "tools/core-fix/validate_l3_batch$N.py"
          done
          echo "L3_COMPLETED_CONTRACTS=PASS"
